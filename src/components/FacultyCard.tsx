import type { Faculty } from '../types'
export default function FacultyCard({ faculty }: { faculty: Faculty }) {
  const initials = faculty.name.replace(/[^A-Z]/g, '').slice(-2)
  // WhatsApp accepts an international number made of digits only. A leading 00 is
  // an alternate international prefix, so normalize it to the expected country code.
  const whatsappPhone = faculty.phone.replace(/\D/g, '').replace(/^00/, '')
  return <article className="glass rounded-2xl p-4"><div className="flex gap-3"><div className="grid size-11 shrink-0 place-items-center rounded-full bg-gradient-to-br from-orange-400 to-rose-400 text-sm font-bold text-white">{initials}</div><div className="min-w-0 flex-1"><h3 className="font-semibold text-slate-800">{faculty.name}</h3><p className="mt-0.5 text-sm text-slate-500">{faculty.designation}</p><p className="mt-1 text-xs font-medium text-slate-500">Room: {faculty.roomNo}</p></div></div><div className="mt-4 flex gap-2"><a href={`tel:${faculty.phone}`} aria-label={`Call ${faculty.name}`} className="flex h-11 flex-1 items-center justify-center rounded-xl bg-orange-50 text-sm font-semibold text-orange-600">Call</a><a href={`mailto:${faculty.email}`} aria-label={`Email ${faculty.name}`} className="flex h-11 flex-1 items-center justify-center rounded-xl bg-sky-50 text-sm font-semibold text-sky-700">Email</a><a href={`https://wa.me/${whatsappPhone}`} target="_blank" rel="noopener noreferrer" aria-label={`Chat with ${faculty.name} on WhatsApp`} className="flex h-11 flex-1 items-center justify-center rounded-xl bg-emerald-50 text-sm font-semibold text-emerald-700"><svg aria-hidden="true" viewBox="0 0 24 24" className="mr-1 size-4 fill-current"><path d="M12.04 2a9.8 9.8 0 0 0-8.32 14.98L2 22l5.18-1.68A9.98 9.98 0 1 0 12.04 2Zm0 17.96a8 8 0 0 1-4.08-1.12l-.3-.18-3.08 1 1-3-.2-.32A8 8 0 1 1 12.04 19.96Zm4.38-5.98c-.24-.12-1.42-.7-1.64-.78-.22-.08-.38-.12-.54.12-.16.24-.62.78-.76.94-.14.16-.28.18-.52.06a6.54 6.54 0 0 1-1.92-1.18 7.2 7.2 0 0 1-1.34-1.66c-.14-.24-.02-.37.1-.49l.36-.42c.12-.14.16-.24.24-.4.08-.16.04-.3-.02-.42-.06-.12-.54-1.3-.74-1.78-.2-.46-.4-.4-.54-.4h-.46c-.16 0-.42.06-.64.3-.22.24-.84.82-.84 2s.86 2.32.98 2.48c.12.16 1.7 2.6 4.12 3.64.58.25 1.03.4 1.38.51.58.18 1.11.15 1.53.09.47-.07 1.42-.58 1.62-1.14.2-.56.2-1.04.14-1.14-.06-.1-.22-.16-.46-.28Z" /></svg>WhatsApp</a></div></article>
}
